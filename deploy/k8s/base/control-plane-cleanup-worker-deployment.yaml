apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane-cleanup-worker
  namespace: runtime-pool
  labels:
    app.kubernetes.io/name: control-plane-cleanup-worker
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: control-plane-cleanup-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: control-plane-cleanup-worker
    spec:
      serviceAccountName: control-plane-worker
      containers:
        - name: cleanup-worker
          image: ghcr.io/example/agent-failure-control-plane
          imagePullPolicy: IfNotPresent
          command:
            - uv
            - run
            - python
            - -m
            - apps.control_plane.src.interfaces.runtime.cleanup_worker
          envFrom:
            - configMapRef:
                name: control-plane-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: runtime-secrets
                  key: DATABASE_URL
          resources:
            requests:
              cpu: "50m"
              memory: "96Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
      imagePullSecrets:
        - name: ghcr-pull
