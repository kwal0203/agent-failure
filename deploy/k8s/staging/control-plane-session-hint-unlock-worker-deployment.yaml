apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane-session-hint-unlock-worker
  namespace: runtime-pool
  labels:
    app.kubernetes.io/name: control-plane-session-hint-unlock-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: control-plane-session-hint-unlock-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: control-plane-session-hint-unlock-worker
    spec:
      containers:
        - name: session-hint-unlock-worker
          image: ghcr.io/kwal0203/agent-failure-control-plane@sha256:1c84d669d4316220bdc13ea3e4ef55102a3bb511a03e1101f95a061f39929a61
          imagePullPolicy: IfNotPresent
          command:
            - uv
            - run
            - python
            - -m
            - apps.control_plane.src.interfaces.runtime.session_hint_unlock_worker
          env:
            - name: DATABASE_URL
              value: "postgresql+psycopg://postgres:localdev@172.17.0.1:5432/agent_failure"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
      imagePullSecrets:
        - name: ghcr-pull
