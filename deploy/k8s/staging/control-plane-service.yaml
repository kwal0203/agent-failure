apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: runtime-pool
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: control-plane
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
