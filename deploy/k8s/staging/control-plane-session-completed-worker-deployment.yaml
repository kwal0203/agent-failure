apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane-session-completed-worker
  namespace: runtime-pool
  labels:
    app.kubernetes.io/name: control-plane-session-completed-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: control-plane-session-completed-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: control-plane-session-completed-worker
    spec:
      containers:
        - name: session-completed-worker
          image: ghcr.io/kwal0203/agent-failure-control-plane
          imagePullPolicy: IfNotPresent
          command:
            - uv
            - run
            - python
            - -m
            - apps.control_plane.src.interfaces.runtime.session_completed_worker
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: runtime-secrets
                  key: DATABASE_URL
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
      imagePullSecrets:
        - name: ghcr-pull
