apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane-evaluator-worker
  namespace: runtime-pool
  labels:
    app.kubernetes.io/name: control-plane-evaluator-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: control-plane-evaluator-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: control-plane-evaluator-worker
    spec:
      containers:
        - name: evaluator-worker
          image: ghcr.io/kwal0203/agent-failure-control-plane
          imagePullPolicy: IfNotPresent
          command:
            - uv
            - run
            - python
            - -m
            - apps.evaluator.src.interfaces.runtime.evaluator_worker
          envFrom:
            - configMapRef:
                name: control-plane-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: runtime-secrets
                  key: DATABASE_URL
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: runtime-secrets
                  key: OPENROUTER_API_KEY
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
      imagePullSecrets:
        - name: ghcr-pull
